title = "cv gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Allow known non-production placeholders"
paths = [
  '''(^|/)docker/alertmanager\.email\.tpl\.yml$''',
  '''(^|/)docker/alertmanager\.prod\.yml$''',
]
regexes = [
  '''replace-with-gmail-app-password''',
  '''\$\{SMTP_AUTH_PASSWORD\}''',
]
