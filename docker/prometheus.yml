global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "cv-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["api:3000"]

rule_files:
  - /etc/prometheus/prometheus.rules.yml